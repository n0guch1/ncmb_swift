Pod::Spec.new do |s|
  s.name         = "NCMB"
  s.version      = "0.1.0"
  s.summary      = "not yet"
  s.description  = <<-DESC
                   not yet
                   DESC
  s.homepage     = "not yet"
  s.license      = "Apache License, Version 2.0"
  s.author       = "not yet"
  s.platform     = :ios, "7.0"
  s.source       = { :git => 'https://github.com/n0guch1/ncmb_swift.git'}
  s.source_files  = "ncmb_swift/*.{swift}"
  s.requires_arc = true
end
